'use babel';

import FirstTestPackageView from '../lib/first-test-package-view';

describe('FirstTestPackageView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
